server:
    port: 8080
    servlet:
        context-path: /identity
spring:
    datasource:
        url: "jdbc:mysql://localhost:3306/indentity-service"
        username: root
        password: root

    jpa:
        hibernate:
        #tự tạo db
            ddl-auto: update
        show-sql: true
    
    output:
        ansi:
            enabled: ALWAYS
